<UserCard Style="background-color: var(--mud-palette-drawer-background);color: var(--mud-palette-drawer-text);"/>

<MudNavMenu>
    <MudNavLink Href="/" Match="NavLinkMatch.All" Class="my-1" Icon="@Icons.Material.Filled.Home">Anasayfa</MudNavLink>
    <MudNavLink Href="/account" Icon="@Icons.Material.Outlined.LockPerson">Hesabım</MudNavLink>
    @if (_canViewRoles || _canViewUsers)
    {
        <MudNavGroup Title="Sistem Yönetimi" Icon="@Icons.Material.Outlined.Security" HideExpandIcon="false">
            @if (_canViewUsers)
            {
                <MudNavLink Href="pages/identity/users" Icon="@Icons.Material.Outlined.Person">Kullanıcılar</MudNavLink>
            }
            @if (_canViewRoles)
            {
                <MudNavLink Href="pages/identity/roles" Icon="@Icons.Material.Outlined.Lock">Roller</MudNavLink>
            }
        </MudNavGroup>
    }
</MudNavMenu>